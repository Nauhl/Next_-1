
  /*Basicamente se realizara lo llamado "route actions / routing" de forma de default para redirijir pero 
  en una forma mas simple y que todas las demas paginas contengan lo de esta pagina.
  In this part we are using "route actions / routing" in a default form to re-direct but in a simple way and the other pages will 
  have the same default attributes.

Import React from 'react';         ******** OLD version ********        */
import { HomePage } from '../containers/Homepage';

export default HomePage;